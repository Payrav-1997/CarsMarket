@model HomeViewModel
<h2>Лучшие автомобили</h2>
<div class="row mt-5 mb-2">
    @{
        foreach (CarModel car in Model.favCars)
        {
            @Html.Partial("AllCars", car)
        }

    }
</div>